# PP-Human/PaddleX 보행자 속성 모델을 Paddle 런타임으로 직접 사용할 때 설치.
# ONNX Runtime wheel이 Jetson/aarch64 환경에서 맞지 않을 경우 대체 경로로 사용한다.
#
# 주의:
# - 런타임에는 paddlepaddle만 필요하다.
# - paddle2onnx/onnxoptimizer는 변환 도구라 운영 이미지에는 넣지 않는다.
#   Jetson/aarch64 빌드에서 onnxoptimizer 소스 빌드가 필요해질 수 있어
#   런타임 이미지 안정성과 빌드 시간을 해친다.

paddlepaddle==3.2.2
