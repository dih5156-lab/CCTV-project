# CCTV 헬멧 착용 및 낙상 감지 시스템 - 필수 패키지

# 딥러닝 프레임워크
ultralytics>=8.0.0,<9.0.0  # YOLOv8 (헬멧, 낙상, 사람 감지)
torch>=2.0.0,<3.0.0  # PyTorch (YOLO 백엔드)
torchvision>=0.15.0,<1.0.0

# 컴퓨터 비전
opencv-python>=4.8.0,<5.0.0  # OpenCV (영상 처리, RTSP)
opencv-contrib-python>=4.8.0,<5.0.0  # OpenCV 추가 기능

# 이미지 처리
numpy>=1.24.0,<3.0.0
Pillow>=10.0.0,<12.0.0

# 서버 통신
requests>=2.31.0,<3.0.0  # HTTP 통신
urllib3>=2.0.0,<3.0.0
paho-mqtt>=1.6.1,<3.0.0  # MQTT (EdgeX MessageBus)
redis>=5.0.0,<6.0.0  # Redis MessageBus (EdgeX 내부 이벤트 전달)
pydantic>=2.5.0,<3.0.0  # speaker-edgex dtos.option 의존성

# 데이터 처리
pandas>=2.0.0,<3.0.0  # 데이터 분석 (선택사항)

# 설정 관리
python-dotenv>=1.0.0,<2.0.0  # 환경 변수 관리

# 로깅
colorlog>=6.7.0,<7.0.0  # 컬러 로그 (선택사항)

# 유틸리티
tqdm>=4.66.0,<5.0.0  # 진행률 표시
psutil>=5.9.0,<7.0.0  # 시스템 모니터링

