# 누적 판정 방식 (Cumulative Detection)

## 개요
실무에서 사용하는 오탐(False Positive) 필터링 기법입니다. 단일 프레임에서 헬멧이 안 보인다고 바로 경고를 울리지 않고, 연속된 여러 프레임의 추론 결과를 누적하여 판정합니다.

## 동작 원리

### 1. 결과 수집
```
프레임 1: 헬멧 없음 (✓)
프레임 5: 헬멧 있음  
프레임 10: 헬멧 없음 (✓)
프레임 15: 헬멧 없음 (✓)
프레임 20: 헬멧 없음 (✓)
```
→ 최근 5번의 추론 결과: [없음, 있음, 없음, 없음, 없음]

### 2. 임계값 판정
- 총 5번 중 **4번 이상**이 "헬멧 없음"이면 **경고 발생**
- 위의 예: 4개가 없음 ≥ 임계값 4 → **경고**

### 3. 필터링 효과
- ✅ 일시적 고개 움직임 무시 (1~2프레임)
- ✅ 모델의 한 번 실수 무시
- ✅ 오탐으로 인한 불필요한 로그 감소

## 설정 파라미터

```python
# src/config/config.py의 ProcessingConfig

cumulative_detection_enabled: bool = True          # 활성화 여부
detection_history_size: int = 5                   # 최근 몇 개 결과를 저장할지
violation_threshold: int = 4                      # 몇 개 이상이 위반이면 경고할지
```

## 사용 예시

### 기본 설정 (권장)
```bash
python main.py --cameras cameras.json --display
# 기본값: 5개 결과 중 4개 이상 위반 시 경고
```

### 더 엄격한 판정 (2개 이상)
```python
# config.py 수정
violation_threshold: int = 2
```

### 더 관대한 판정 (모두 위반해야 경고)
```python
# config.py 수정
violation_threshold: int = 5
```

### 누적 판정 비활성화 (기존 방식)
```python
# config.py 수정
cumulative_detection_enabled: bool = False
```

## 로그 메시지

### 경고 발생
```
[camera_1] 객체 123: 누적 판정 결과 위반 (4/5) -> no_helmet
```

### 경고 미발생
```
[camera_1] 객체 123: 누적 판정 진행 중 (2/5) - 아직 경고 아님
```

## 권장 설정

| 환경 | frame_skip | history_size | threshold | 설명 |
|------|-----------|--------------|-----------|------|
| 엄격 | 3 | 5 | 4 | 빠른 감지, 높은 오탐율 |
| 표준 | 8 | 5 | 4 | 균형잡힌 설정 (권장) |
| 관대 | 15 | 7 | 5 | 느린 감지, 낮은 오탐율 |

## 효과

### Before (누적 판정 없음)
```
14:23:01 경고: 헬멧 미착용 감지 (신뢰도 0.65)
14:23:02 경고: 헬멧 미착용 감지 (신뢰도 0.68)  ← 고개 움직임
14:23:03 경고: 헬멧 미착용 감지 (신뢰도 0.32)  ← 모델 오류
14:23:04 정상
14:23:05 경고: 헬멧 미착용 감지 (신뢰도 0.71)
```
→ **쓸모없는 경고 많음, 실제 위반 구분 어려움**

### After (누적 판정 적용)
```
14:23:04 정상
14:23:05 경고: 누적 판정 결과 위반 (4/5)
```
→ **확신 있는 경고만 발생**

## 주의사항

1. **frame_skip과 균형**
   - frame_skip이 크면 누적 판정 효과 감소
   - frame_skip: 8, history_size: 5 권장

2. **카메라 특성별 튜닝**
   - 해상도 낮음: threshold 높이기 (4→5)
   - 빠른 움직임 많음: history_size 줄이기 (5→3)

3. **이벤트 전송
   - 누적 판정 후 필터링된 이벤트만 서버로 전송
   - 불필요한 네트워크 트래픽 감소

