# Action layer (cctv-action-layer / cctv-alert-api / cctv-edgex-adapter) dependencies
# Used by custom CCTV services in docker-compose.yml
# (No AI inference - torch/ultralytics excluded)

# HTTP
requests>=2.31.0
urllib3>=2.0.0
httpx>=0.27.0

# MQTT
paho-mqtt>=1.6.1

# Redis
redis>=5.0.0

# DTO validation
pydantic>=2.5.0

# Public API (FastAPI + ASGI server)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Configuration
python-dotenv>=1.0.0

# Logging
colorlog>=6.7.0
